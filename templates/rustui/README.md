# makeit - rustui template

makeit template for creating rust TUI application.

It uses `rust` template as base and adds basic code, which is used by basically
every TUI application, so you don't have to write it over and over.

## Dependencies

- [`cargo`](https://doc.rust-lang.org/cargo/getting-started/installation.html)
